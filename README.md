# 丁保堂个人电子名片（第一个）

这是一个可直接部署到 GitHub Pages 的静态电子名片网站。

## 本地预览

```bash
python -m http.server 4173
```

然后访问 `http://localhost:4173/`。

## GitHub Pages 发布

把仓库推送到 GitHub 后，在仓库的 `Settings` -> `Pages` 中选择：

- Source: `Deploy from a branch`
- Branch: `main`
- Folder: `/ (root)`

保存后，GitHub 会生成公开访问链接。
